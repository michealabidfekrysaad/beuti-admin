import { defineMessages } from 'react-intl';

const scope = 'language';

export const messages = defineMessages({
  english: {
    id: `${scope}.english`,
  },
  arabic: {
    id: `${scope}.arabic`,
  },
});
