/*
 * Footer Copy Right Messages
 */
import { defineMessages } from 'react-intl';

export const scope = 'components.footer';

export default defineMessages({
  copyRights: {
    id: `${scope}.copyRights`,
  },
});
